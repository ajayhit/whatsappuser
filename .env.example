NODE_ENV=production
SESSION_DIR=./sessions
DB_PATH=./database.db
# PostgreSQL online connection string (e.g. Neon, Supabase, Render PostgreSQL)
DATABASE_URL=postgresql://username:password@host:5432/dbname?sslmode=require
